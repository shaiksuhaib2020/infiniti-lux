## GLOBAL COLOR UPDATE — Apply Before Turn 4

Before building anything new, the agent makes this change across the entire codebase first.

---

### Background Color Change — All White Sections

**Replace `--color-white` section backgrounds with `#EDE7D9` throughout.**

Looking at the screenshot, the white sections are reading too sterile for a premium travel brand. `#EDE7D9` is a warm sand — it adds richness, depth and the feeling of quality paper stock. Think luxury travel magazine, not SaaS dashboard.

**Update the design token in `globals.css`:**

```
--color-page: #EDE7D9;       ← NEW: warm sand, replaces white for section backgrounds
--color-ice: #E4DDD0;         ← UPDATED: slightly deeper sand for alternating sections
--color-surface: #FFFFFF;     ← KEEP: card surfaces, form fields, inputs — stay white so they float
```

**Precise rule for the agent — three categories:**

| What it is | Before | After |
|---|---|---|
| `body` background | `#FFFFFF` | `#EDE7D9` |
| Section backgrounds (white sections) | `#FFFFFF` | `#EDE7D9` |
| Section backgrounds (ice sections) | `#F5F7FA` | `#E4DDD0` |
| Card backgrounds | `#FFFFFF` | **Stay `#FFFFFF`** — cards must pop against the sand |
| Form field inputs | `#FFFFFF` | **Stay `#FFFFFF`** |
| Navbar (scrolled state) | `#07111C` | **Unchanged** |
| Footer | `#07111C` | **Unchanged** |
| Dark sections (abyss, deep-navy) | Unchanged | **Unchanged** |
| Dropdown (Travel nav) | `#FFFFFF` | **Stay `#FFFFFF`** — needs to read as a floating surface |
| Mobile nav popover | `#07111C` | **Unchanged** |
| FAQ accordion container | `#FFFFFF` | **Stay `#FFFFFF`** — it's a card surface |
| Visa destination cards | `#FFFFFF` | **Stay `#FFFFFF`** |

**Why this distinction matters:** The entire visual logic of the site is now: warm sand page → white cards float on top → dark navy anchors key moments. Cards being white against `#EDE7D9` creates far more visual lift than white cards on a white page — which is partly why the screenshot looks flat in places.

**File sweep the agent must do:**
Every CSS module that has `background: var(--color-white)` or `background: #FFFFFF` on a section or page wrapper needs to be updated to `var(--color-page)` or `var(--color-ice)` respectively. Card and input backgrounds stay explicitly `#FFFFFF`.

Also update the `TripSearch` box: its outer section wrapper becomes `--color-page`, but the search card itself stays `#FFFFFF`.

---

---

# TURN 4 — ABOUT US PAGE BRIEF
## Infiniti Luxe | Agent Instruction Document

---

## PART 0 — DESIGN INTENT FOR THIS PAGE

The About Us page has one job: make a first-time visitor trust Infiniti Luxe enough to hand over their travel plans. This is a premium brand story page — not a company history timeline, not a team directory, not a list of certifications. It is an **editorial statement of values and intent**, told through typography, photography and considered layout.

The tone is confident without being boastful. Honest without being self-deprecating. The copy from the requirements doc is already well-written and should be used verbatim in its designated section — it does not need rewriting.

**File:** `app/about/page.jsx`

**Component folder:** `components/about/` — sub-components for this page.

---

## PART 1 — PAGE STRUCTURE

```
1. About Hero
2. The Manifesto
3. What We Do
4. Our Values
5. Based in Dubai
6. Final CTA
```

---

## PART 2 — SECTION SPECIFICATIONS

---

### Section 1 — About Hero

**Height:** `65vh` — this page is about words and ideas more than photography. The hero is intentional but not dominating.

**Background treatment — not a photograph.** This is a deliberate departure from every other page. The hero is a **pure dark surface** (`--color-abyss`), with a very subtle repeating texture — a fine dot grid pattern, SVG-based, white at 3% opacity. This gives depth without a photograph. The visual focus is entirely on the typography.

**Why no photo here:** Every other hero on the site uses destination photography. The About page hero using text-only on a deep dark surface creates immediate distinction — it signals that this page is different in nature. It is a statement, not a scene.

**Content — centered, vertically centered:**

1. Small label — `--font-body`, 12px, `--color-gold`, weight 500, letter-spacing 0.06em. Text: "About Infiniti Luxe". Sentence case. 

2. Headline — `--font-display`, 68px desktop / 46px tablet / 36px mobile, white, weight 300, line-height 1.08, letter-spacing -0.025em, centered, max-width 760px:
"We Believe Travel Is More Than Reaching a Destination."

3. Supporting line — `--font-body`, 17px, white at 65% opacity, centered, max-width 540px, line-height 1.65, margin-top 20px:
"Based in Dubai. Built to help people travel with confidence."

**No CTA buttons in the hero.** The page invites reading, not immediate action. The CTA comes at the end once trust has been earned.

**GSAP entrance:** Headline fades in and rises 20px over 0.8s, `power2.out`. Label fades in simultaneously. Supporting line follows with 0.2s delay. The whole entrance is slightly slower than other pages — this is a considered, editorial page.

**Dot grid SVG texture:** The agent generates an inline SVG pattern — a 20×20px repeating unit containing a single `1px` circle at center, white `fill-opacity="0.03"`. Applied as a `background-image` on the hero container. Positioned behind all content via `z-index`. This is subtle — if you squint, you can barely see it. That's correct.

---

### Section 2 — The Manifesto

**Background:** `--color-page` (`#EDE7D9`) — warm sand, full width.

**Section padding:** 120px top and bottom — more generous than standard. This section needs room to breathe.

**Layout:** Centered, single column, max-width `680px`. Text is the only content here — there are no cards, no icons, no supporting elements. Restraint is the design decision.

**Content:**

The copy from the requirements document is used verbatim, broken into typographic moments:

---

**Opening line — typographic feature:**

"We believe travel is more than reaching a destination."

Style: `--font-display`, 40px, `--color-ink`, weight 400, line-height 1.2, centered. This line is larger than the body text that follows — it functions as a section opener, a thesis statement. No label above it. Just the line.

---

**Body paragraphs — 3 paragraphs, 32px gap between each:**

Paragraph 1:
"It is the anticipation before the journey, the places you discover, the people you meet and the memories you bring home."

Paragraph 2:
"Based in Dubai, we are building a modern travel company designed to make exploring the world simpler, more personal and more memorable. From flights and hotels to holiday packages, tours, experiences and visa assistance, we help travellers bring their plans together in one place."

Paragraph 3:
"Whether you're planning a family holiday, a honeymoon, a business trip, a luxury escape or your next adventure, our team is here to help you travel with confidence."

Style for all paragraphs: `--font-body`, 18px, `--color-slate`, line-height 1.85, centered. At 18px with line-height 1.85, this reads like a quality editorial publication. The warm sand background behind it reinforces that quality.

---

**Closing statement — typographic feature:**

"Your destination is out there. Let us help you get there."

Style: `--font-display`, 32px, `--color-ink`, weight 400, italic, centered, margin-top 48px. The italic treatment of the closing line gives it a different register — it shifts from a statement of fact to something closer to a personal commitment. One of the few places italics are used in the entire site.

---

**ScrollTrigger on this section:** The opening large line fades in first. Then the three paragraphs stagger in at 0.15s intervals. Then the closing italic line fades in last with a 0.1s delay after the final paragraph. The section reads like a letter, revealed line by line.

---

### Section 3 — What We Do

**Background:** `--color-ice` (`#E4DDD0`) — a half-step deeper than the page sand, creating a gentle section break without a hard border or full color change.

**Section padding:** 96px top and bottom.

**Section header — left-aligned within max-width container:**

- Heading: "What We Offer" — `--font-display`, 44px, `--color-ink`, weight 400
- Subtext: "One team for every part of your journey." — `--font-body`, 16px, `--color-stone`, margin-top 12px

**40px gap below the header.**

**Services layout — horizontal scrollable row:**

Not a card grid. A **horizontal row of 7 service items** that scrolls on mobile and sits in a single row on desktop. This is a more editorial treatment than a grid — it feels like a magazine spread of what the brand covers.

Each service item:
- Width: approximately `160px` fixed, no stretching
- No card border, no shadow — these are minimal identity blocks, not clickable cards (though they do link)
- Top: a thin `2px` horizontal bar in `--color-gold`, width `32px`, margin-bottom `16px`. This is the only decorative element. It acts as a category marker.
- Service name: `--font-display`, 22px, `--color-ink`, weight 400, line-height 1.2
- One line descriptor: `--font-body`, 13px, `--color-stone`, margin-top 6px
- Route link: routes to the respective service page on click of the whole block

**On hover (whole block):** The `2px` gold bar extends from `32px` to `48px` via CSS transition (`width: 0.3s ease`). Subtle, precise, premium.

**The 7 service items:**

| Service | Descriptor | Route |
|---|---|---|
| Flights | Getting you there | `/travel/flights` |
| Hotels | Where you stay | `/travel/hotels` |
| Holidays | Complete packages | `/travel/holidays` |
| Visa Services | Document assistance | `/visa-services` |
| Tours | Local experiences | `/travel/tours` |
| Cruises | The world by sea | `/travel/cruises` |
| Corporate | Business travel | `/corporate-travel` |

**ScrollTrigger:** Items stagger in left to right, 0.08s per item, as the section enters the viewport.

---

### Section 4 — Our Values

**Background:** `--color-page` (`#EDE7D9`) — back to warm sand.

**Section padding:** 96px top and bottom.

**Section header — centered:**

- Heading: "What We Stand For" — `--font-display`, 44px, `--color-ink`, weight 400
- No subtext — let the values speak immediately

**48px gap below the header.**

**Layout:** 3 columns on desktop, 1 column on mobile. 3 values total — an intentionally small number. Three clear things are more memorable than six generic ones.

**Each value block:**

- Value name: `--font-display`, 28px, `--color-ink`, weight 400
- Body: `--font-body`, 15px, `--color-slate`, line-height 1.75, max-width 300px, margin-top 12px

Between the value name and the body text: a `40px` tall section of whitespace — more than typical. This gives each value room and weight.

**No icons, no numbers, no borders.** The values float on the sand background. This is the most minimal section on the site and intentionally so — values stated plainly, without decoration, feel more genuine than values inside icon boxes.

**The 3 values:**

| Value | Body |
|---|---|
| Personal Service | You are not a booking reference. You are a traveller with a specific trip in mind. We plan around you, not around what's easiest to sell. |
| Complete Honesty | We do not make promises we cannot keep. That includes visa outcomes, pricing and what we are and are not able to arrange. |
| One Team, One Trip | From the first conversation to the day you return, the same team handles your travel. No handoffs, no confusion. |

**ScrollTrigger:** The three blocks reveal together as a group — not staggered. They are parallel, equal values, not a sequence.

---

### Section 5 — Based in Dubai

**Background:** `--color-abyss` (`#07111C`) — full dark section.

**Section padding:** 96px top and bottom.

**Layout:** Two columns, 50/50, on desktop. Stacked on mobile.

**Left column — text:**

Label — `--font-body`, 12px, `--color-gold`, weight 500, letter-spacing 0.05em: "Where we are"

Heading — `--font-display`, 44px, white, weight 400, line-height 1.15, margin-top 16px:
"Based in Dubai. Travelling the World."

Body — `--font-body`, 16px, white at 70% opacity, line-height 1.75, max-width 440px, margin-top 20px:
"Dubai sits at the intersection of East and West — one of the world's great hubs for international travel. It is where we are, and it is the best possible base from which to help people travel everywhere else."

Second paragraph — same style:
"From here, we arrange travel across Europe, Asia, the Americas, Africa, the Middle East and the Pacific. Wherever you want to go, we know the routes."

**Contact details block — 32px below the second paragraph:**

Three clickable contact rows. Each row:
- Icon (Lucide): `--color-gold`, size 18px
- Detail text: `--font-body`, 15px, white at 85% opacity

| Icon | Text | Action |
|---|---|---|
| `Phone` | +971 58 210 9797 | `tel:+971582109797` |
| `Mail` | infinitiempire0007@gmail.com | `mailto:infinitiempire0007@gmail.com` |
| `MapPin` | Dubai, UAE | No link — text only for Phase 1 |

Hover on clickable rows: text becomes `--color-gold`. Smooth `0.2s` transition.

**Right column — a visual card:**

A dark card (`--color-deep-navy` background, radius `--radius-large`, padding `40px`) containing three geographic reach stats — each a **typographic moment, not a metric claim.** These are factual scope statements, not invented numbers.

| Display value | Label |
|---|---|
| 14+ | Destinations we package |
| Worldwide | Flight routes we book |
| 7 days | Available to assist |

Display value style: `--font-display`, 48px, `--color-gold`, weight 300
Label style: `--font-body`, 13px, white at 55% opacity, weight 400

`48px` gap between each stat. A `1px solid --color-border-dark` separator line between each stat.

**ScrollTrigger:** Left column text block fades in. Right column card fades in and rises 20px, 0.15s after the left column starts.

---

### Section 6 — Final CTA

**Background:** `--color-deep-navy` (`#0D2035`) — distinct from the `--color-abyss` section above it but still dark, creating a graduated dark close to the page.

**Section padding:** 96px top and bottom.

**Content — centered, max-width 600px:**

1. Heading — `--font-display`, 52px, white, weight 300, line-height 1.1:
"Ready to Plan Your Next Journey?"

2. Supporting line — `--font-body`, 17px, white at 70% opacity, line-height 1.6, margin-top 16px:
"Whether it's a family holiday, a honeymoon, a business trip or something still taking shape — talk to us. We'll help you figure it out."

3. Two buttons, centered, 16px gap, margin-top 40px:
   - **Primary:** Background `--color-whatsapp`, white text, weight 600, padding `14px 28px`, radius `--radius-button`. Text: "Start on WhatsApp". Link: `https://wa.me/971582109797?text=Hi%20Infiniti%20Luxe%2C%20I%20would%20like%20help%20planning%20my%20trip.`
   - **Secondary:** Transparent, `1px solid rgba(255,255,255,0.40)`, white text, same sizing. Text: "View Our Services". Routes to `/`.

4. Reassurance line — `--font-body`, 13px, white at 40% opacity, centered, margin-top 24px:
"No pressure, no commitment. Just a real conversation about your travel plans."

---

## PART 3 — DESIGN PRINCIPLES SPECIFIC TO THIS PAGE

### Typography is the design

The About page has fewer visual elements than any other page on the site. The `--font-display` Cormorant Garamond at varying sizes is doing almost all the visual work. The agent must ensure:

- The manifesto section (Section 2) is generous with line-height and padding — **do not compress it**
- The closing italic line is genuinely italic (`font-style: italic`) — not oblique, not simulated
- The values section (Section 4) has more vertical space between value name and body than feels "normal" — this is intentional. Resist the urge to tighten it.

### Color rhythm across the page

| Section | Background |
|---|---|
| Hero | `--color-abyss` (dark) |
| Manifesto | `--color-page` `#EDE7D9` (warm sand) |
| What We Do | `--color-ice` `#E4DDD0` (deeper sand) |
| Values | `--color-page` `#EDE7D9` (warm sand) |
| Based in Dubai | `--color-abyss` (dark) |
| Final CTA | `--color-deep-navy` (dark) |

Dark → Sand → Deeper Sand → Sand → Dark → Dark

The page opens and closes in darkness. The middle is warmth. This arc mirrors the emotional journey — arrive, understand, trust, commit.

### No statistics without evidence

The only numbers on this page are in the "Based in Dubai" section: `14+ destinations`, `Worldwide routes`, `7 days`. These are operational scope statements, not performance claims. The agent must not add any review counts, customer numbers, or years-in-business figures.

### ScrollTrigger — lightest touch on this page

This is the most restrained animation brief of all pages:

- Hero entrance: as specified above
- Section 2 (Manifesto): sequential paragraph reveal — the most elaborate animation on this page
- Section 3 (Services): left-to-right stagger at 0.08s
- Section 4 (Values): reveal together as a group
- Section 5 (Dubai): left then right, 0.15s offset
- Section 6 (CTA): single fade, no stagger

No parallax, no movement on scroll, no sticky elements. The page is still and considered. Motion would break the editorial tone.

---

## PART 4 — METADATA AND ROUTING

**Document metadata:**

```
Title: "About Us — Infiniti Luxe"
Description: "Based in Dubai, Infiniti Luxe is a modern global travel company offering flights, holidays, hotels, visa assistance and more. Travel with confidence."
```

**Active nav state:**

`/about` is a top-level PillNav item. `activeHref` passed to PillNav matches the About item with the gold indicator when on this route.

---

## SUMMARY — WHAT THE AGENT BUILDS IN THIS TURN

**Global change first (before any new code):**
- Update `--color-page` to `#EDE7D9` across all light section backgrounds
- Update `--color-ice` to `#E4DDD0`
- Keep card surfaces, form fields, dropdowns, and popover components at `#FFFFFF`
- Sweep all CSS modules and update section/page wrapper backgrounds

**Then build:**

1. `app/about/page.jsx`
2. Sub-components in `components/about/`:
   - `AboutHero.jsx` — dark with dot grid SVG texture
   - `Manifesto.jsx` — sequential typographic reveal
   - `WhatWeDo.jsx` — horizontal scrollable service strip
   - `OurValues.jsx` — three minimal floating value blocks
   - `DubaiPresence.jsx` — two-column dark section with contact details
   - `AboutCTA.jsx` — dark closing CTA
3. Metadata export
4. Active nav state for `/about`
5. All ScrollTrigger animations as specified — restrained

